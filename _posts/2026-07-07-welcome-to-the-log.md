---
title: "Welcome to the log"
tags: [meta]
---

This is the first entry. Nothing technical here yet — just a placeholder to
show how a post looks once it's published.

A few things worth knowing about how this works:

- Every post lives in `_posts/` as a markdown file named `YYYY-MM-DD-title.md`.
- The block at the top between the `---` lines is the **front matter** — it sets the title and tags.
- Regular markdown (headings, lists, code blocks, links, images) all work in the body.

```python
def hello():
    print("this is what a code block looks like")
```

Delete this post once you've got real entries going, or keep it around as a
reference for the front matter format.
